spring:
  application:
    name: t1-java-hw
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://postgresdb:5432/test_db
    driverClassName: org.postgresql.Driver
    username:
    password:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8085